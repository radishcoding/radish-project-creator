root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go", "yaml", "yml"]
  exclude_dir = ["tmp", "bin", "docs", "deployments", "api"]
  delay = 500
