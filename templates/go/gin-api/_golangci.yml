run:
  timeout: 5m
linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gosec
    - revive
    - gocritic
    - misspell
    - bodyclose
    - noctx
issues:
  exclude-rules:
    - path: _test\.go
      linters: [gosec, noctx]
